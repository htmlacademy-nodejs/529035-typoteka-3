'use strict';

const DEFAULT_COMMAND = `--help`;
const USER_PARAMS_START = 2;

const EXIT_CODE = {
  success: 0,
  error: 1
};

module.exports = {
  DEFAULT_COMMAND,
  USER_PARAMS_START,
  EXIT_CODE,
};
